// @deps
/*
 * Be the Cell: calibrated level constants (LEVELS §3.7).
 *
 * GENERATED by tools/level-calibrate.js, which re-measures every number the
 * level files read on the current engine. That tool arrives with level 1.2
 * (build step 3); until then this file is a hand-written placeholder with no
 * level sections, and nothing that is shipped reads a calibrated value (the
 * Prologue needs none). Test L-11 fails when engineVersion differs from
 * BTC.ENGINE_VERSION, so an engine bump forces a re-calibration.
 */
(function (root, factory) {
  if (typeof module === 'object' && module.exports) {
    module.exports = factory();
  } else {
    var B = root.BTC || (root.BTC = {});
    B.levelConstants = factory();
  }
})(typeof self !== 'undefined' ? self : this, function () {
  'use strict';
  return Object.freeze({
    engineVersion: '1.1.0',
    generated: null,
    seeds: 0,
    placeholder: true,
  });
});
